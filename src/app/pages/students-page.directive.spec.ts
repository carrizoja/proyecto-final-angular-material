import { StudentsPageDirective } from './students-page.directive';

describe('StudentsPageDirective', () => {
  it('should create an instance', () => {
    const directive = new StudentsPageDirective();
    expect(directive).toBeTruthy();
  });
});
