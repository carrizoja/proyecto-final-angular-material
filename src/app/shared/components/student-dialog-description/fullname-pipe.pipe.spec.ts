import { FullnamePipePipe } from '../shared/components/student-dialog-description/fullname-pipe.pipe';

describe('FullnamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FullnamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
